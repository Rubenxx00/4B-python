# carichiamo da codice una lista di interi "casuali", una lista di parole (di stringhe)
# se la lista di interi contiene il numero 1, unire le due liste in un'unica nuova lista che contine prima quella numerica e poi quella di stringhe,
# altrimenti nell'ordine contrario